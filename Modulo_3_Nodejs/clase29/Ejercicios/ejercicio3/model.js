// Función que recibe dos números, los suma y devuelve el resultado
const addNumbers = (num1, num2) => {
    return num1 + num2;  // Realizamos la suma de los dos números
};

// Exportamos la función para que otros archivos puedan utilizarla
module.exports = {
    addNumbers,
};
