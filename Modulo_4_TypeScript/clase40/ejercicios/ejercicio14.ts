// Declarar una tupla con un valor opcional
let conductor: [string, number, boolean?];

// Inicializar la tupla
conductor = ["Carlos", 28];

// Añadir el valor de licencia de conducir
conductor[2] = true;

// Mostrar la tupla actualizada
console.log(conductor);
