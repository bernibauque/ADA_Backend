// Declarar una tupla para representar las coordenadas
let coordenadas: [number, number] = [10, 15];

// Desestructurar la tupla
let [x, y] = coordenadas;

// Imprimir los valores
console.log(x);  // 10
console.log(y);  // 15
